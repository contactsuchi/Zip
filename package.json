{
  "name": "zip",
  "version": "1.0.0",
  "type": "module",

  "scripts": {
    "lint": "eslint",
    "test": "jest --ci"
  },

  "dependencies": { 
  
  },

  "devDependencies": {
    "eslint": "^10.0.0",
    "jest":"30.2.0"
  }
}
